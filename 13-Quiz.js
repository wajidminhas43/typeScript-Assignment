"use strict";
let transportMode = ["Car", "bus", "motorbike", "bicycle"];
transportMode.forEach(function (value) {
    // let uppercase =toUpperCase()
    console.log(`"My Favorite transport is ${value}"`);
});
